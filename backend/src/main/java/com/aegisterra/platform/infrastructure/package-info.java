/**
 * Infrastructure layer: persistence, security adapters, external systems.
 * Implements application ports. Concrete adapters arrive with persistence and JWT phases.
 */
package com.aegisterra.platform.infrastructure;
